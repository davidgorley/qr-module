version: '3.8'

services:
  qr-module:
    build: .
    container_name: qr-module
    ports:
      - "5000:5000"
      - "2575:2575"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
