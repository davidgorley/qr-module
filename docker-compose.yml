version: '3.8'

services:
  qr-image-transfer:
    build: .
    container_name: qr-image-transfer
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
